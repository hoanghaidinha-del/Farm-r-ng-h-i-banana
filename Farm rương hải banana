--[[
    👑 AUTHOR: HẢI8A
    🍌 BANANA HUB (ẨN/HIỆN KHÔNG DỪNG FARM)
]]

if game.CoreGui:FindFirstChild("Hai8aHub") then
    game.CoreGui.Hai8aHub:Destroy()
end

-- ================= BIẾN TOÀN CỤC =================
getgenv().AutoFarm = false
getgenv().AntiAFK = true

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

-- ================= LOGIC FARM (CHẠY ĐỘC LẬP) =================
local function getRoot()
    return Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
end

task.spawn(function()
    while task.wait(0.1) do
        if getgenv().AutoFarm then
            local root = getRoot()
            if root then
                for _, v in pairs(workspace:GetDescendants()) do
                    if not getgenv().AutoFarm then break end
                    
                    local name = v.Name:lower()
                    if name:find("chest") or name:find("rương") or name:find("treasure") or name:find("box") then
                        local target = v:IsA("BasePart") and v or v:FindFirstChildWhichIsA("BasePart", true)
                        
                        if target and target.Parent then
                            root.CFrame = target.CFrame + Vector3.new(0, 3, 0)
                            pcall(function()
                                firetouchinterest(root, target, 0)
                                firetouchinterest(root, target, 1)
                                for _, p in pairs(v:GetDescendants()) do
                                    if p:IsA("ProximityPrompt") then fireproximityprompt(p) end
                                end
                                local cd = v:FindFirstChildWhichIsA("ClickDetector", true)
                                if cd then fireclickdetector(cd) end
                            end)
                            task.wait(0.3)
                        end
                    end
                end
            end
        end
    end
end)

-- ================= GIAO DIỆN CHÍNH =================
local Gui = Instance.new("ScreenGui", game.CoreGui)
Gui.Name = "Hai8aHub"
Gui.ResetOnSpawn = false

local Main = Instance.new("Frame", Gui)
Main.Size = UDim2.new(0, 400, 0, 250)
Main.Position = UDim2.new(0.5, -200, 0.5, -125)
Main.BackgroundColor3 = Color3.fromRGB(15,15,15)
Main.BorderSizePixel = 0
Main.Active = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 15)
Instance.new("UIStroke", Main).Color = Color3.fromRGB(255,215,0)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1,0,0,50)
Title.Text = "HẢI8A HUB 🍌 - MOBILE"
Title.TextColor3 = Color3.fromRGB(255,215,0)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.BackgroundTransparency = 1

local FarmBtn = Instance.new("TextButton", Main)
FarmBtn.Size = UDim2.new(0.8,0,0,50)
FarmBtn.Position = UDim2.new(0.1,0,0.45,0)
FarmBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
FarmBtn.Text = "AUTO FARM: TẮT"
FarmBtn.TextColor3 = Color3.new(1,1,1)
FarmBtn.Font = Enum.Font.GothamBold
FarmBtn.TextSize = 16
Instance.new("UICorner", FarmBtn)

FarmBtn.MouseButton1Click:Connect(function()
    getgenv().AutoFarm = not getgenv().AutoFarm
    FarmBtn.Text = getgenv().AutoFarm and "AUTO FARM: ĐANG CHẠY" or "AUTO FARM: TẮT"
    FarmBtn.BackgroundColor3 = getgenv().AutoFarm and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(30,30,30)
end)

-- ================= NÚT TRÒN BẬT/TẮT (KHÔNG DỪNG FARM) =================
local ToggleIcon = Instance.new("TextButton", Gui)
ToggleIcon.Size = UDim2.new(0, 50, 0, 50)
ToggleIcon.Position = UDim2.new(0.05, 0, 0.15, 0)
ToggleIcon.Text = "🍌"
ToggleIcon.TextSize = 30
ToggleIcon.BackgroundColor3 = Color3.fromRGB(255,215,0)
ToggleIcon.BorderSizePixel = 0
ToggleIcon.ZIndex = 10
Instance.new("UICorner", ToggleIcon).CornerRadius = UDim.new(1,0)
local StrokeIcon = Instance.new("UIStroke", ToggleIcon)
StrokeIcon.Thickness = 2
StrokeIcon.Color = Color3.new(0,0,0)

-- Click để ẩn hiện
ToggleIcon.MouseButton1Click:Connect(function()
    Main.Visible = not Main.Visible
end)

-- Kéo thả nút tròn (Dành riêng cho Mobi)
local dragIconStart, startIconPos
ToggleIcon.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragIconStart = input.Position
        startIconPos = ToggleIcon.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragIconStart and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragIconStart
        ToggleIcon.Position = UDim2.new(startIconPos.X.Scale, startIconPos.X.Offset + delta.X, startIconPos.Y.Scale, startIconPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragIconStart = nil
    end
end)

-- ================= KÉO THẢ MENU CHÍNH =================
local dragStart, startPos
Main.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragStart = input.Position
        startPos = Main.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragStart and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragStart = nil
    end
end)

-- Anti AFK
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    task.wait(1)
    game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
end)
